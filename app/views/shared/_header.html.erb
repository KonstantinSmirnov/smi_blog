<nav class="navbar">
  <div class="blog-logo">
    <%= link_to "SMIPLAY", root_path, class: 'navbar-brand' %>
  </div>
  <div class="blog-social-buttons hidden-sm-down">
    <%= render :partial => 'shared/social_buttons' %>
  </div>
  <div class="blog-links">
    <button class="navbar-toggler hidden-sm-up" type="button" data-toggle="collapse" data-target="#exCollapsingNavbar2" aria-controls="exCollapsingNavbar2" aria-expanded="false" aria-label="Toggle navigation">
      &#9776;
    </button>
    <div class="collapse navbar-toggleable-xs" id="exCollapsingNavbar2">
      <ul class="nav navbar-nav">
        <li class="nav-item <%= 'active' if controller?("articles") %>">
          <%= link_to "Blog", articles_path, { :class => 'nav-link' } %>
        </li>
        <li class="nav-item <%= 'active' if controller?("about") %>">
          <%= link_to "About", about_path, { :class => 'nav-link' } %>
        </li>
        <% if admin_signed_in? %>
          <li class="nav-item <%= 'active' if namespace?("admin") %>">
            <%= link_to "Admin", admin_dashboard_path, { :class => 'nav-link' } %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign out", destroy_admin_session_path, { data: { confirm: 'Are you sure to sign out?' }, :class => 'nav-link' } %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
